import { TaskInterface } from "./task.interface";

interface UserInterface {
  id: number;
  name: string;
  firstName: string;
  lastName: string;
  email: string;
  icon?: string;
}

export default UserInterface;
